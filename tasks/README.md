# Implementation Tasks

Use this directory for bounded, reviewable implementation specifications. Copy `TEMPLATE.md`, give the file a stable descriptive name, and fill every relevant section before non-trivial coding.

A task must agree with current requirements, architecture, and ADRs. If it changes architecture, write and accept an ADR first. Keep completed task files as decision context unless the team adopts a separate tracker with durable links.
